import { Contract } from 'zos-lib';
export default class EventsFilter {
    timeout: number;
    constructor(timeout?: number);
    call(contract: Contract, eventName?: string): Promise<any>;
    _promiseTimeout(promise: Promise<any>): Promise<any>;
}
