declare const DEFAULT_TX_TIMEOUT: number;
interface SessionOptions {
    network?: string;
    from?: string;
    timeout?: number;
}
declare const Session: {
    getOptions(overrides?: SessionOptions): SessionOptions;
    open({ network, from, timeout }: SessionOptions, expires?: number): void;
    close(): void;
    ignoreFile(): void;
    _parseSession(): SessionOptions;
    _setDefaults(session: SessionOptions): SessionOptions;
};
export { DEFAULT_TX_TIMEOUT };
export default Session;
