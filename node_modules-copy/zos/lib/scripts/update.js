"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const stdout_1 = __importDefault(require("../utils/stdout"));
const ControllerFor_1 = __importDefault(require("../models/network/ControllerFor"));
const ScriptError_1 = __importDefault(require("../models/errors/ScriptError"));
function update({ packageName, contractAlias, proxyAddress, initMethod, initArgs, all, network, force = false, txParams = {}, networkFile }) {
    return __awaiter(this, void 0, void 0, function* () {
        if (!packageName && !contractAlias && !proxyAddress && !all) {
            throw Error('The package name, contract name, or address to update must be provided, or set the `all` flag to update all contracts in the application.');
        }
        const controller = ControllerFor_1.default(network, txParams, networkFile);
        try {
            yield controller.checkLocalContractsDeployed(!force);
            const proxies = yield controller.upgradeProxies(packageName, contractAlias, proxyAddress, initMethod, initArgs);
            proxies.forEach((proxy) => stdout_1.default(proxy.address));
            controller.writeNetworkPackageIfNeeded();
        }
        catch (error) {
            const cb = () => controller.writeNetworkPackageIfNeeded();
            throw new ScriptError_1.default(error, cb);
        }
    });
}
exports.default = update;
//# sourceMappingURL=update.js.map