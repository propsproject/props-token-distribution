import { UpdateParams } from './interfaces';
export default function update({ packageName, contractAlias, proxyAddress, initMethod, initArgs, all, network, force, txParams, networkFile }: UpdateParams): Promise<void>;
