import { VerifyParams } from './interfaces';
export default function verify(contractAlias: any, { network, txParams, networkFile, optimizer, optimizerRuns, remote, apiKey }: VerifyParams): Promise<void | never>;
