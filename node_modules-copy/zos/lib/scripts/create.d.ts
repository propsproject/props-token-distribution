import { CreateParams } from './interfaces';
import { Contract } from 'zos-lib';
export default function createProxy({ packageName, contractAlias, initMethod, initArgs, network, txParams, force, networkFile }: CreateParams): Promise<Contract | never>;
